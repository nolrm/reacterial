# pnpm configuration

# Hoist all dependencies to the root node_modules
shamefully-hoist=true

# Store node_modules in project directory
node-linker=hoisted

# Auto-install peers
auto-install-peers=true

# Strict peer dependencies (can be set to false if issues arise)
strict-peer-dependencies=false

